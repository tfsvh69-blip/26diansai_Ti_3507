# 消息列表

当前工程暂未启用跨任务消息。

当前 IMU 姿态输出由 `IMU100Hz` 任务直接调用 `module/imu` 与 `bsp/imu_port` 接口，并通过 UART0 调试输出，暂未发布为跨任务消息。

后续新增跨任务或跨应用通信时，应优先使用 FreeRTOS queue、event group、stream buffer 或项目统一消息中心，并在本文件记录：

| 消息名 | 结构体 | 发布者 | 订阅者 | 用途 |
|---|---|---|---|---|
| 暂无 | 暂无 | 暂无 | 暂无 | 暂无 |
